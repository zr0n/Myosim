{
  // Required
  "manifest_version": 3,
  "name": "Myosim",
  "version": "0.1",
  // Recommended
  "description": "Simulate myopia on user browser. Ctrl + M to set the level.",
  "content_scripts": [{
    "matches": ["*://*/*"],
    "js": ["myosim.js"]
  }],
  "permissions": [
    "storage"
  ]
}